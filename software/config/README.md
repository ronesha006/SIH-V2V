# Configuration

Contains configurable parameters used by the software.

Examples:

- Vehicle ID
- Lane ID
- Safety thresholds
- Distance thresholds
- Heartbeat interval
- Communication timeout
- Test mode
- Simulation mode

Configuration should be centralized instead of hardcoding values throughout the code.

Example:

OBSTACLE_WARNING_DISTANCE
OBSTACLE_CRITICAL_DISTANCE
HEARTBEAT_TIMEOUT